/**
 * Object Preview
 *
 * Draw the path that projectile will do
 **/
 var Preview = {
	name: 'Preview',
	r: 0.5, // Preview arcs' radius
	show: true, // Indicate if the preview must be shown

	// Time (miliseconds) between every draw loop. See Helper->draw()
	step: 0.1
};
